!!!не привязывай к домену, сделай демо параллелизма

Нам необходимо реализовать дополнительное задание по параллельному коду. Давай выберем Задача В (Asyncio): конкурентная асинхронная подгрузка данных (имитация подгрузки тяжелых прайс-листов или текстур с помощью asyncio.sleep).

Твоя задача:

    Создать отдельный Python-модуль pharmacy/concurrency.py.

    Написать асинхронную функцию, которая имитирует конкурентный запрос к 5 виртуальным серверам поставщиков за данными о наличии лекарств (используй asyncio.gather и asyncio.sleep).

    Создать отдельную страницу на сайте (без CSS оформления), при переходе на которую запускается этот асинхронный процесс, замеряется время выполнения, и результаты выводятся на экран. Для запуска asyncio внутри синхронного Django используй asgiref.sync.async_to_sync или отдельный поток с event loop.

Предоставь код concurrency.py, код представления и HTML-шаблона.

Дополнительно приведу тебе изначальные пункты из ТЗ, связанные с данными задачами, но делай то, что указано выше:
26. Дополнительное задание: дополнить проект модулем для работы с параллельным кодом (можно использовать asyncio, multiprocessing или multithreading на выбор); добавить отдельную страницу сайта без css оформления для демонстрации решения задачи.
Общие требования к реализации:
- Задача А (Threading): Сконцентрироваться на работе с общими данными и использовании примитивов синхронизации
(Lock, Semaphore). Задача: потоки обновляют сетевые координаты 5 игроков.
- Задача Б (Multiprocessing): использовать большие объемы данных (списки от элементов), чтобы оправдать накладные
расходы на создание процессов. Задача: Расчет векторов столкновения для 105 объектов на карте.
- Задача В (Asyncio): Реализовать конкурентное выполнение большого количества легких сетевых задач (имитация
через asyncio.sleep). Задача: асинхронная подгрузка текстур с сервера во время игры.

Нам необходимо реализовать дополнительное задание по работе с параллельным и асинхронным кодом (Пункт 26 ТЗ). Мы выбираем "Задача В (Asyncio): Асинхронная подгрузка текстур с сервера во время игры".

Эта задача должна быть реализована в виде изолированной демо-страницы на нашем сайте. Она не связана с логикой аптеки, а моделирует поведение игрового движка при конкурентной загрузке ресурсов.

Выполни следующие шаги:

1. Создай отдельный Python-модуль `pharmacy/concurrency.py`. Внутри него реализуй:
   - Асинхронную функцию (например, `fetch_texture(texture_name, size_mb)`), которая имитирует скачивание отдельной тяжелой текстуры с игрового сервера с помощью `await asyncio.sleep()` (время ожидания должно зависеть от размера "текстуры").
   - Главную асинхронную функцию (например, `load_game_assets()`), которая формирует список из 10-15 различных игровых текстур (например: 'skybox.hdr', 'terrain_diffuse.png', 'player_skin.tga', 'weapon_normal.png' и т.д.) и запускает их конкурентную загрузку через `asyncio.gather()`.
   - Функция должна замерять суммарное время выполнения асинхронного процесса, а также рассчитывать, сколько времени заняла бы последовательная загрузка (сумма всех sleep), чтобы наглядно показать выигрыш в производительности.

2. Создай представление (View) в `pharmacy/views.py`:
   - Напиши контроллер (например, `concurrency_demo_view`), который вызывает асинхронный код. Для запуска асинхронного event loop внутри синхронного Django используй `asgiref.sync.async_to_sync` или механизм управления потоками с новым event loop, чтобы избежать конфликтов с основным потоком веб-сервера.
   - Передай результаты симуляции (список загруженных текстур, их размер, статус "Успешно загружено", время старта/окончания для каждой, общее асинхронное время и гипотетическое последовательное время) в контекст шаблона.

3. Создай HTML-шаблон (например, `templates/pharmacy/concurrency_demo.html`):
   - Строго по ТЗ: страница должна быть БЕЗ CSS оформления (чистый, дефолтный HTML-вывод: списки, таблицы, стандартные кнопки).
   - Выведи на ней заголовок: "Демонстрация Задача В (Asyncio): Асинхронная подгрузка текстур с сервера во время игры".
   - Отобрази лог загрузки: какая текстура в какой поток/задачу ушла, сколько весила и как быстро подгрузилась конкурентно.
   - Выведи итоговую аналитику: "Общее время асинхронной загрузки: X сек. Время последовательной загрузки: Y сек. Эффективность: +Z%".

4. Добавь маршрут в `pharmacy/urls.py` для доступа к этой странице (используй re_path с регулярным выражением, например, r'^concurrency-demo/$').

Предоставь полный код для файлов `concurrency.py`, `views.py`, `urls.py` и HTML-шаблона.

Ты — ведущий Django-разработчик. Нам нужно реализовать первый этап проекта "Автоматизация Аптеки".
Базовый каркас проекта PharmacyProject и приложение pharmacy уже созданы. В качестве БД используется SQLite.
Твоя задача:
    В pharmacy/models.py спроектировать и написать следующие модели:
        Индивидуальные (Аптека): Medication (код, название, инструкция, описание, стоимость, фото, категория - M2M или FK), PharmacyDepartment (отдел), Employee (сотрудник, связь 1:1 с User, отдел), Supplier (поставщик), Sale (продажа: медикамент, сотрудник, количество, дата, сумма), MedicationCategory.
        Общие страницы: Article (для главной и новостей), CompanyInfo, Glossary (FAQ), Vacancy, Review (отзывы с оценкой), PromoCode (активные/архивные).

    Реализовать типы связей: OneToOneField (Employee -> User), ForeignKey (Sale -> Employee), ManyToManyField (Medication -> MedicationCategory / Supplier).

    Зарегистрировать ВСЕ модели в pharmacy/admin.py. Настроить фильтрацию, поиск и встроенное редактирование связанных записей (Inline для продаж внутри сотрудника или медикаментов внутри категорий).

    Напиши Python-скрипт или кастомную management-команду для наполнения БД фикстурами (минимум 10 товаров и заполненные данные для страниц "О компании", "Контакты" и т.д.).

Важно: Не используй сторонние CSS фреймворки (Bootstrap запрещен). Код должен соответствовать PEP 8. Предоставь код файлов models.py, admin.py и команды для фикстур.

Дополнительно приведу тебе изначальные пункты из ТЗ, связанные с данными задачами, но делай то, что указано выше:
1. Определить необходимые сущности для описания предметной области. Реализовать их в терминах
моделей, используя подходящие типы данных и связи объектов (теория https://developer.mozilla.org/ru/docs/Learn/Server-side/Django/Models). Представить предметную область на диаграмме в виде, как на прикреплённом изображении. В качестве базы данных использовать sqlite.
Индивидуальное задание: предметная область "аптека". Ориентировочные модели, которые можно создать: Медикаменты, Сотрудники, Продажи, Поставщики, Отделы аптеки, Категории медикаментов. Основная цель проекта, которую нужно обеспечить: автоматизация работы аптеки, основным направлением которой является продажа медикаментов разных видов.
Нужно предусмотреть возможность хранения и изменения данных для обеспечения функционала в соответствии с индивидуальным заданием и для страниц, которые должны быть в каждом приложении независимо от индивидуального задания. Эти страницы полностью будут реализованы в последующем, поэтому сейчас следует ограничиться их наполнением через базу данных или админ панель.
Список необходимых страниц для всех заданий (то, что можно выполнить по желанию отмечено***):
– Главная: краткая информация о последней опубликованной статье
– О компании: информация о компании – просто текст (*** видео, логотип, история по годам, реквизиты – пока
только сделать таблицу в базе данных) (таблица в базе данных)
– Новости: список статей в соответствии с тематикой сайта с заголовком, кратким содержанием
(одно предложение), картинкой (таблица в базе данных) (*** и кнопкой «Читать далее» при нажатии на
которую открывается вся статья)
– Словарь терминов и понятий: список часто задаваемых вопросов и ответов на них с датой
добавления на сайт (таблица в базе данных)
– Контакты: фотографии сотрудников с описанием выполняемых работ, номерами телефона, почтовыми адресами и т.д.
(таблица в базе данных)
– Политика конфиденциальности: пока добавить пустую страницу
– Вакансии: список вакансий с описанием (таблица в базе данных)
– Отзывы: список отзывов с указанием имени, оценки, текста, даты (таблица в базе данных).
(Кнопка добавить отзыв с переходом к окну регистрации или в личный кабинет залогиненного
пользователя. При нажатии кнопки открытие формы с полем для текста отзыва и выбора
оценки, кнопкой «Отправить», которая сохраняет отзыв в базе).
– Промокоды и купоны: список промокодов действующих и в архиве. (таблица в базе данных).
2. Индивидуальное задание:
- На сайте аптеки для админа (superuser) отображается:
возможность добавлять и редактировать информацию о медикаментах(код, название, инструкция, описание, стоимость, фото), отделах аптеки, сотрудниках и поставщиках,
информацию о всех продажах выбранных медикаментов,
информация о конкретном лечащем средстве, о конкретном сотруднике, общей выручке со всех продаж и по отделам.
- На сайте отображается для User без регистрации:
Инфо о категориях медикаментов и самих медикаментах с фильтрацией по цене, доп. услугах, промокоды, купоны
- На сайте отображается для User с регистрацией "сотрудник"
Инфо о продажах и поставщиках с которыми работает + всё, что видно без регистрации
- На сайте отображается для User с регистрацией "покупатель"
имеет возможность купить товар, видит инфо о точках самовывоза, покупках и промокодах + всё, что видно без регистрации
3. Реализовать связи один к одному (OneToOneField), один ко многим (ForeignKey) и многие ко многим
(ManyToManyField).
(5. Добавить все модели в админ панель, создать суперюзера, обеспечить работу с данными,
фильтрацию, встроенное редактирование связанных записей
(теоретический материал для Django по ссылке https://developer.mozilla.org/ru/docs/Learn/Server-side/Django/Admin_site)
8. Обеспечить наполнение данными для демонстрации не менее 10 записей в списке товаров/услуг/объектов/клиентов!
